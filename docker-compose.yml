services:
  karriar:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - karriar-data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/karriar.db

volumes:
  karriar-data:
